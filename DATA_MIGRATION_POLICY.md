# سياسة حفظ بيانات اليوسفي سوفت

## قاعدة التحديث

تحديث APK لا يحذف بيانات التطبيق. يجب أن يبقى `applicationId` ثابتاً:
`com.alyousifisoft.app`. في Android، التحديث فوق النسخة المثبتة يحتفظ بمجلد بيانات التطبيق، بما فيه
SQLite عبر Capacitor و`localStorage` وIndexedDB اللذان يستخدمهما التطبيق حالياً.

لا يجوز لأي إصدار مستقبلي أن يستدعي `deleteDatabase` أو `clear()` أو يحذف مجلد بيانات التطبيق
أثناء التحديث. إعادة الضبط الموجودة داخل التطبيق إجراء اختياري صريح من المستخدم وليست جزءاً من
تثبيت أو تحديث APK.

## سياسة المخطط

- ارفع `SCHEMA_VERSION` فقط عند وجود تغيير فعلي في شكل البيانات.
- اجعل كل ترقية تحويلية وغير مدمرة: أضف القيم الافتراضية للحقول الجديدة واترك المصفوفات
  القديمة كما هي.
- لا تستبدل البيانات الموجودة ببيانات تجريبية عند وجود سجل محفوظ.
- حافظ على مفاتيح التخزين الحالية (`alyousifi-soft-state` و`alyousifi-soft-local`) وجدول
  SQLite المحلي، أو اكتب ترقية صريحة تنقل البيانات قبل تغييرها.
- اختبر ترقية نسخة سابقة تحتوي على مبيعات وعملاء وديون ومخزون قبل نشر APK جديد.
- زد `versionCode` لكل APK جديد، وزد `versionName` عند نشر إصدار مرئي جديد.

## الإصدار الحالي

- إصدار التطبيق: `1.0.15`
- رقم البناء: `5`
- إصدار مخطط البيانات: `3`
